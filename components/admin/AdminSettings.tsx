'use client';

import { useEffect, useState } from 'react';
import { Check, Save } from 'lucide-react';

type SettingsState = { storeName: string; storeEmail: string; storeDescription: string; adminEmail: string; timezone: string; currency: string; lowStockThreshold: number; maintenanceMode: boolean };
const defaults: SettingsState = { storeName: 'Above Viewing', storeEmail: '', storeDescription: '', adminEmail: '', timezone: 'Asia/Manila', currency: 'PHP', lowStockThreshold: 5, maintenanceMode: false };

export function AdminSettings() {
  const [settings, setSettings] = useState(defaults); const [saved, setSaved] = useState(false);
  useEffect(() => { try { const raw = localStorage.getItem('aboveview-admin-settings'); if (raw) setSettings({ ...defaults, ...JSON.parse(raw) }); } catch {} }, []);
  const set = (key: keyof SettingsState, value: unknown) => setSettings(prev => ({ ...prev, [key]: value }));
  const save = (e: React.FormEvent) => { e.preventDefault(); localStorage.setItem('aboveview-admin-settings', JSON.stringify(settings)); setSaved(true); window.setTimeout(() => setSaved(false), 2200); };
  const input = 'mt-2 w-full rounded-lg border border-[#d9dad4] bg-[#fbfbf8] px-3 py-2.5 text-sm outline-none focus:border-[#74784f]';
  return <form onSubmit={save} className="max-w-4xl space-y-6"><section className="rounded-xl border border-[#deded8] bg-white p-5 sm:p-6"><h2 className="font-semibold">Store information</h2><p className="mt-1 text-sm text-[#85898a]">The details shown across your storefront.</p><div className="mt-5 grid gap-4 sm:grid-cols-2"><label className="text-xs font-semibold text-[#555a5e]">Store name<input className={input} value={settings.storeName} onChange={e => set('storeName', e.target.value)} /></label><label className="text-xs font-semibold text-[#555a5e]">Store email<input type="email" className={input} value={settings.storeEmail} onChange={e => set('storeEmail', e.target.value)} /></label><label className="text-xs font-semibold text-[#555a5e] sm:col-span-2">Store description<textarea rows={3} className={input} value={settings.storeDescription} onChange={e => set('storeDescription', e.target.value)} /></label></div></section><section className="rounded-xl border border-[#deded8] bg-white p-5 sm:p-6"><h2 className="font-semibold">Account & security</h2><p className="mt-1 text-sm text-[#85898a]">Admin account preferences for this workspace.</p><div className="mt-5 grid gap-4 sm:grid-cols-2"><label className="text-xs font-semibold text-[#555a5e]">Admin email<input type="email" className={input} value={settings.adminEmail} onChange={e => set('adminEmail', e.target.value)} /></label><label className="text-xs font-semibold text-[#555a5e]">Timezone<select className={input} value={settings.timezone} onChange={e => set('timezone', e.target.value)}><option>Asia/Manila</option><option>UTC</option><option>America/New_York</option><option>Europe/London</option></select></label></div><p className="mt-5 rounded-lg bg-[#f4f4ef] p-3 text-xs text-[#777b80]">Password changes use the existing admin authentication configuration. Store this page’s preferences securely before deploying.</p></section><section className="rounded-xl border border-[#deded8] bg-white p-5 sm:p-6"><h2 className="font-semibold">Store preferences</h2><div className="mt-5 grid gap-4 sm:grid-cols-2"><label className="text-xs font-semibold text-[#555a5e]">Currency<select className={input} value={settings.currency} onChange={e => set('currency', e.target.value)}><option>USD</option><option>PHP</option><option>EUR</option></select></label><label className="text-xs font-semibold text-[#555a5e]">Low stock threshold<input type="number" min="0" className={input} value={settings.lowStockThreshold} onChange={e => set('lowStockThreshold', Number(e.target.value))} /></label></div><label className="mt-5 flex items-center gap-3 text-sm text-[#555a5e]"><input type="checkbox" checked={settings.maintenanceMode} onChange={e => set('maintenanceMode', e.target.checked)} className="h-4 w-4 accent-[#74784f]" /> Enable storefront maintenance mode</label></section><button className="inline-flex items-center gap-2 rounded-lg bg-[#74784f] px-5 py-3 text-sm font-semibold text-white hover:bg-[#626741]">{saved ? <Check className="h-4 w-4" /> : <Save className="h-4 w-4" />}{saved ? 'Saved' : 'Save settings'}</button></form>;
}
