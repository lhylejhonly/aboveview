'use client';

import { useState } from 'react';
import { Edit3, Plus, Trash2, X } from 'lucide-react';
import { useAdmin, CategoryMeta } from '@/context/AdminContext';

export function CategoryManagement() {
  const { categories, products, addCategory, updateCategory, deleteCategory } = useAdmin();
  const [editing, setEditing] = useState<CategoryMeta | null>(null); const [adding, setAdding] = useState(false); const [label, setLabel] = useState(''); const [id, setId] = useState(''); const [deleting, setDeleting] = useState<string | null>(null);
  const openAdd = () => { setEditing(null); setLabel(''); setId(''); setAdding(true); }; const openEdit = (c: CategoryMeta) => { setEditing(c); setLabel(c.label); setId(c.id); setAdding(true); };
  const save = async (e: React.FormEvent) => { e.preventDefault(); const value = { id: id.trim().toLowerCase().replace(/\s+/g, '-'), label: label.trim() }; if (!value.id || !value.label) return; if (editing) await updateCategory(value); else await addCategory(value); setAdding(false); };
  return <div className="space-y-5"><div className="flex items-center justify-between"><div><p className="text-sm text-[#777b80]">Organize products into manageable collections.</p></div><button onClick={openAdd} className="inline-flex items-center gap-2 rounded-lg bg-[#74784f] px-4 py-2.5 text-sm font-semibold text-white hover:bg-[#626741]"><Plus className="h-4 w-4" /> Add category</button></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{categories.map(category => { const count = products.filter(p => p.category === category.id).length; return <div key={category.id} className="rounded-xl border border-[#deded8] bg-white p-5"><div className="flex items-start justify-between"><div><h2 className="font-semibold">{category.label}</h2><p className="mt-1 font-mono text-[11px] text-[#929695]">{category.id}</p></div><div className="flex gap-1"><button onClick={() => openEdit(category)} className="rounded-md p-2 text-[#74784f] hover:bg-[#edf0e3]"><Edit3 className="h-4 w-4" /></button><button onClick={() => setDeleting(category.id)} className="rounded-md p-2 text-[#ad6250] hover:bg-[#f7e9e4]"><Trash2 className="h-4 w-4" /></button></div></div><div className="mt-7 flex items-end justify-between"><span className="text-xs uppercase tracking-[0.14em] text-[#85898a]">Products</span><span className="text-2xl font-semibold">{count}</span></div></div>; })}</div>{adding && <div className="fixed inset-0 z-[60] flex items-center justify-center bg-[#11151a]/60 p-5"><form onSubmit={save} className="w-full max-w-md rounded-xl bg-white p-6 shadow-2xl"><div className="flex items-center justify-between"><h2 className="text-lg font-semibold">{editing ? 'Edit category' : 'Add category'}</h2><button type="button" onClick={() => setAdding(false)}><X className="h-5 w-5 text-[#777b80]" /></button></div><label className="mt-6 block text-xs font-semibold text-[#555a5e]">Category name<input required value={label} onChange={e => setLabel(e.target.value)} className="mt-2 w-full rounded-lg border border-[#d9dad4] px-3 py-2.5 text-sm outline-none focus:border-[#74784f]" /></label><label className="mt-4 block text-xs font-semibold text-[#555a5e]">Category ID<input required disabled={!!editing} value={id} onChange={e => setId(e.target.value)} className="mt-2 w-full rounded-lg border border-[#d9dad4] px-3 py-2.5 text-sm outline-none disabled:bg-[#f1f1ed] focus:border-[#74784f]" /></label><div className="mt-6 flex justify-end gap-3"><button type="button" onClick={() => setAdding(false)} className="rounded-lg border border-[#d9dad4] px-4 py-2 text-sm">Cancel</button><button className="rounded-lg bg-[#74784f] px-4 py-2 text-sm font-semibold text-white">{editing ? 'Save changes' : 'Add category'}</button></div></form></div>}{deleting && <div className="fixed inset-0 z-[70] flex items-center justify-center bg-[#11151a]/60 p-5"><div className="w-full max-w-sm rounded-xl bg-white p-6 shadow-2xl"><h2 className="font-semibold">Delete category?</h2><p className="mt-2 text-sm text-[#777b80]">Products assigned to it will keep their current category value.</p><div className="mt-6 flex justify-end gap-3"><button onClick={() => setDeleting(null)} className="rounded-lg border border-[#d9dad4] px-4 py-2 text-sm">Cancel</button><button onClick={async () => { await deleteCategory(deleting); setDeleting(null); }} className="rounded-lg bg-[#ad6250] px-4 py-2 text-sm font-semibold text-white">Delete</button></div></div></div>}</div>;
}

